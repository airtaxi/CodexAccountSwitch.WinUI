<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="CliAccountSwitcher.WinUI.Dialogs.AddZaiAccountDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dev="using:DevWinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:seg="using:CliAccountSwitcher.WinUI.ThirdParty.SelectorBarSegmented"
    x:Uid="AddZaiAccountDialog"
    Closing="OnAddZaiAccountDialogClosing"
    DefaultButton="Close"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">
    <ContentDialog.Resources>
        <ResourceDictionary>
            <x:Double x:Key="ContentDialogMaxWidth">1200</x:Double>
        </ResourceDictionary>
    </ContentDialog.Resources>

    <Grid Width="760" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <seg:SelectorBarSegmented
            x:Name="AddAccountModeSelectorBar"
            Grid.Row="0"
            HorizontalAlignment="Center"
            SelectedItem="{x:Bind CurrentAccountSelectorBarItem}"
            SelectionChanged="OnAddAccountModeSelectorBarSelectionChanged">
            <SelectorBarItem
                x:Name="CurrentAccountSelectorBarItem"
                x:Uid="AddZaiAccountDialog_CurrentAccountSelectorBarItem"
                Tag="CurrentAccount" />
            <SelectorBarItem
                x:Name="ApiKeySelectorBarItem"
                x:Uid="AddZaiAccountDialog_ApiKeySelectorBarItem"
                Tag="ApiKey" />
        </seg:SelectorBarSegmented>

        <Frame
            x:Name="AddAccountContentFrame"
            Grid.Row="1"
            Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}" />
    </Grid>
</ContentDialog>
